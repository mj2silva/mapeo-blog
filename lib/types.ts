export type MeetingInfo = {
  id?: string,
  names: string,
  email:string,
  phoneNumber: string,
  company: string,
  subject: string,
  date: Date,
}

export type User = {
  id?: string,
  username: string,
  email: string,
}
